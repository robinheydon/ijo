

#ifdef DEFINE_COMPONENTS
    #define DEFINE_STRUCT(name, type_desc)\
        ECS_COMPONENT_DECLARE (name);\
        ECS_STRUCT(name, type_desc)
#else
    #define DEFINE_STRUCT(name, type_desc)\
        extern ECS_COMPONENT_DECLARE (name);\
        ECS_STRUCT_TYPE(name, type_desc)
#endif

DEFINE_STRUCT (Position, {
    float x;
    float y;
});

DEFINE_STRUCT (Velocity, {
    float dx;
    float dy;
});

DEFINE_STRUCT (Tree, {
    float growth;
});



